# KanbanPrinter - Configuration
# Copiez ce fichier vers .env et remplissez vos valeurs

# === OpenAI API ===
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-5.2

# === Imprimante ===
# Nom exact tel qu'affiché dans les paramètres Windows
PRINTER_NAME=Munbyn ITPP941

# === Dimensions des étiquettes (en mm) ===
# 2" x 1" = 50.8mm x 25.4mm
LABEL_WIDTH_MM=51
LABEL_HEIGHT_MM=25

# === DPI de l'imprimante ===
# Munbyn ITPP941 supporte généralement 203 DPI
PRINTER_DPI=203

# === Google Tasks (optionnel) ===
# Chemin vers le fichier credentials.json de l'API Google
GOOGLE_CREDENTIALS_PATH=config/google_credentials.json
